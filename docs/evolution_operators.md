# Evolution Operators

The current mutation operator registry is explicit and deterministic.

Implemented operators:

- `repair_scope`
- `add_falsifier`
- `branch_counterexample`
- `combine_independent_support`

These operators are recorded for planning and artifacts. Full automated generation of child hypotheses remains a later milestone.
