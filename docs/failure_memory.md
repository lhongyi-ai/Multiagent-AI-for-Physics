# Failure Memory

Failure memory stores negative knowledge from cheap kill tests and failed gates.

Examples:

- unfalsifiable branch
- no predictions
- unbounded claim
- dimensionally invalid statement

Live Agent Meeting now receives failure memory through `meeting_tool_context.json` so agents can avoid repeating branches that were already killed.
