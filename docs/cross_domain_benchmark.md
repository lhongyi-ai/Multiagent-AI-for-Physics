# Cross-Domain Benchmark

This checkpoint adds deterministic benchmark cases inside each Domain Pack.

Current purpose:

- prove pack discovery
- prove generic acquisition can run offline
- expose guardrails
- provide a future target for optimizer comparisons

The benchmark smoke does not claim scientific validation of any domain.
